function [dom] = getdomain(f)
% GETDOMAIN - Returns object in domain over which the vector field is defined. 
% function [dom] = getdomain(f)

% WRITTEN BY       : Kenth Engø, 1997 Oct.
% LAST MODIFIED BY : Kenth Engø, 2000.04.10

dom = f.domain;
return;

