%-*-text-*-
function [v] = hasshape(a)
% HASSHAPE - Returns 'true' since LGTANGENT is dynamically subtyped.
% function [v] = hasshape(a)

% WRITTEN BY       : Kenth Engø, 2000 Mar.
% LAST MODIFIED BY : None

v = 1;
return;
