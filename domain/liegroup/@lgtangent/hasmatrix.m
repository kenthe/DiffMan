%-*-text-*-
function [v] = hasmatrix(a)
% HASMATRIX - Checks if LGTANGENT has a matrix representation.
% function [v] = hasmatrix(a)

% WRITTEN BY       : Kenth Engø, 2000 Mar.
% LAST MODIFIED BY : None

v = lgthasmatrix(a.shape);
return;

