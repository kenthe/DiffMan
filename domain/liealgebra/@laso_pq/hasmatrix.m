function [v] = hasmatrix(a)
% HASMATRIX - Returns 'true' since the LASO_PQ has a matrix representation.
% function [v] = hasmatrix(a)

% WRITTEN BY       : Kenth Engø, 1999 Apr.
% LAST MODIFIED BY : None

v = 1;
return;
