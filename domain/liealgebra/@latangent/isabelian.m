%-*-text-*-
function [v] = isabelian(a)
% ISABELIAN - Returns 'true' if LATANGENT is Abelian.
% function [v] = isabelian(a)

% WRITTEN BY       : Kenth Engø, 2000 Mar.
% LAST MODIFIED BY : None

v = latisabelian(a.shape);
return;
